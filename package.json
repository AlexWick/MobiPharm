{
  "name": "MobiPharm",
  "version": "1.0.0",
  "description": "A app for a Pharmacy",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm run build-functions",
    "build-functions": "cd functions && npm install && npm run build",
    "deploy": "firebase deploy",
    "build-webapp": "cd webapp && npm install && npm run build",
    "lint": "eslint"
  },
  "dependencies": {
    "18": "^0.0.0",
    "body-parser": "^1.20.2",
    "bootstrap": "^5.2.3",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "firebase": "^9.19.1",
    "firebase-admin": "^11.5.0",
    "firebase-functions": "^4.2.1",
    "nvm": "^0.0.4",
    "upgrade": "^1.1.0"
  },
  "devDependencies": {
    "eslint-config-google": "^0.14.0",
    "firebase-functions-test": "^3.0.0"
  }
}
